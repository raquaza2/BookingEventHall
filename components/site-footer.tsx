export function SiteFooter() {
  return (
    <footer className="mt-20 border-t border-ink/10 py-8">
      <div className="shell flex flex-col gap-3 text-sm text-ink/60 sm:flex-row sm:items-center sm:justify-between">
        <p>Grand Aurora Hall booking system MVP.</p>
        <p>Built for responsive venue discovery, booking requests, and admin review.</p>
      </div>
    </footer>
  );
}

